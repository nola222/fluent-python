# 字符串表示形式
```python
# 内置函数repr：把一个对象用字符串的形式表达出来(__repr__) 若向量没有实现__repr__，那么打印出来为一个object指向内存空间地址
# 用%r替代__repr__返回的对象
# __repr__所返回的字符串应该是准确、无歧义，且尽可能表达出如何用代码创建出这个被打印的对象。这里使用了类似调用对象构造器的表达形式（Vector(3, 4)）
# %和str.format函数字符串格式化语法
# __repr__与__str__区别：后者是在str()函数被使用或者print函数打印一个对象时才会被调用，返回的字符串对终端用户友好。只想实现一个时，前者更佳。
```